version: "3.8"
services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - SECRET_KEY=dev-secret
      - SQLALCHEMY_DATABASE_URI=sqlite:///instance/app.db
    volumes:
      - .:/app
